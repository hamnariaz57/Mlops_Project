pandas==2.1.4
numpy==1.26.4
requests==2.32.3

# MLflow & Experiment Tracking
mlflow>=3.0.0
# NOTE: dagshub conflicts with ydata-profiling due to incompatible dacite requirements
# dagshub requires dacite~=1.6.0, while ydata-profiling requires dacite>=1.8
# If you need dagshub, install it separately: pip install dagshub==0.6.3 --no-deps
# dagshub==0.6.3
# DVC & Remote Storage
dvc==3.48.1
dvc-s3==3.1.0

# Profiling / EDA
ydata-profiling==4.18.0

# Visualization
matplotlib==3.8.2
seaborn==0.12.2

# Environment Variables
python-dotenv==1.0.1

# Machine Learning
scikit-learn==1.4.0
pydantic>=2.11.7,<3.0.0

# AWS SDK (for S3)
boto3==1.34.34

# Backend / API
fastapi==0.110.0
uvicorn[standard]==0.29.0
prometheus-fastapi-instrumentator==7.0.0

# Additional MLflow & DVC dependencies (Required for Python 3.12)
cloudpickle==3.1.2
databricks-cli==0.18.0
protobuf>=5.26.1,<6.0.0
grpcio-tools<1.71
# Note: protobuf 5.x satisfies grpcio-status, mlflow 3.x, and grpcio-tools<1.71
# grpcio-tools>=1.71 requires protobuf 6.x which conflicts with mlflow
sqlparse==0.5.0
alembic==1.11.1
docker==6.1.3
# Flask is provided by Airflow, don't override
# Flask==3.0.0
pyarrow==14.0.2
markdown==3.5.1
waitress==2.1.2
treelib==1.8.0
pathvalidate==3.3.1
dagshub-annotation-converter==0.1.15
gql[requests]==4.0.0
